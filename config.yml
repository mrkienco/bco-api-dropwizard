
server:
#  softNofileLimit: 1000
#  hardNofileLimit: 1000
  applicationConnectors:
    - type: http
      port: 8090
    #this requires the alpn-boot library on the JVM's boot classpath
    #- type: h2
    #  port: 8445
    #  keyStorePath: example.keystore
    #  keyStorePassword: example
    #  validateCerts: false
    #  validatePeers: false
  adminConnectors:
    - type: http
      port: 8091

# Logging settings.
logging:

  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO

# Logback's Time Based Rolling Policy - archivedLogFilenamePattern: /tmp/application-%d{yyyy-MM-dd}.log.gz
# Logback's Size and Time Based Rolling Policy -  archivedLogFilenamePattern: /tmp/application-%d{yyyy-MM-dd}-%i.log.gz
# Logback's Fixed Window Rolling Policy -  archivedLogFilenamePattern: /tmp/application-%i.log.gz

  appenders:
    - type: console
    - type: file
      threshold: INFO
      logFormat: "%-6level [%d{HH:mm:ss.SSS}] [%t] %logger{5} - %X{code} %msg %n"
      currentLogFilename: /tmp/application.log
      archivedLogFilenamePattern: /tmp/application-%d{yyyy-MM-dd}-%i.log.gz
      archivedFileCount: 7
      timeZone: UTC
      maxFileSize: 10MB
      
# db config for dbengine
db_config:
    host: 127.0.0.1
    port: 3306
    user: chaulh
    password: "@pipogame#"
    dbname: banca

# default client config fields
default_client_config:
    server_primary_host: 125.212.192.94
    server_primary_port: 3014
    server_backup_host: 125.212.192.94
    server_backup_port: 3014
    upload_avatar: "http://125.212.192.94/FileUpload/ProfilePicture"
    url_card_api: "http://203.113.159.204:8080/bai69_cardrecharge/payment.php"
    orbit_url: "http://p2o.wiimob.com/gameconfig/banca/orbit.json"
    fan_page: "https://www.facebook.com/casino.vip.69/?fref=ts"
